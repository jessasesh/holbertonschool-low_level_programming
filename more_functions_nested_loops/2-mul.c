#include "main.h"
#include <unistd.h>
/**
 * mul - prototype that mutiplies 2 integers
 * @a: first integer
 * @b: second integer
 * Return: result of equation
 */
int mul(int a, int b)
{
	return (a * b);
}
